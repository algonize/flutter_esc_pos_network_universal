name: flutter_esc_pos_network_universal
description: The library allows printing receipts using ESC/POS commands using
  thermal Wi-Fi/Ethernet printer.
version: 1.0.1
homepage: https://github.com/algonize/flutter_esc_pos_network_universal

environment:
  sdk: ">=3.2.3 <4.0.0"
  flutter: ">=3.41.6"

dependencies:
  flutter:
    sdk: flutter
  flutter_esc_pos_utils: ^1.0.1
  image: ^4.8.0
  screenshot: ^3.0.0
  universal_html: ^2.3.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter: null
